@using dApp.Spectral
@* @using dApp.Spectral


@if (Prams != null)
{
    <div class="flex justify-between items-center pt-4 border-t border-gray-700">
        <span class="text-sm text-gray-400">Volume</span>
        <div id="volumeMeter" class="flex items-center text-sm text-green-400">
            <i class="fas fa-volume-up mr-1"></i>
            <span>0.00</span>
        </div>
    </div>
}

@code {
    [Parameter] public ListDataVolumevoicess? Prams { get; set; }

   
}
 *@
 @if (Prams != null)
{
<div class="flex justify-between items-center pt-4 border-t border-gray-700">
    <span class="text-sm text-gray-400">@Prams.DataBuild.Name</span>
    <div class="flex items-center text-sm text-green-400">
        <i class="@Prams.DataBuild.Icon mr-1"></i>
        <span>@Prams.DataBuild.Volume.ToString("0.00")</span>
    </div>
</div>
}
@code {
    [Parameter] public ListDataVolumevoicess? Prams { get; set; }
}
